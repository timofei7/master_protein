this is a  server using flask

preqeruisites:
   brew install redis 
   pip install flask
   pip install rq

to run it on http://localhost simply do:
   ./run 

test files in:  testing
    testing/test.py
